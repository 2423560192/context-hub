# Skills

这里存放可复用的 Agent Skill。

每个 Skill 使用独立 Markdown 文件，并遵循 [`docs/knowledge-spec.md`](../docs/knowledge-spec.md) 规范。

推荐按领域分类：

```text
skills/
├─ python/
├─ backend/
├─ devops/
├─ agent/
└─ general/
```

原则：Skill 要解决一个明确问题，正文只放真正需要注入 Agent 上下文的内容。
