{
  "mcpServers": {
    "skillhub": {
      "command": "uv",
      "args": [
        "--directory",
        "${workspaceFolder}",
        "run",
        "skillhub-mcp",
        "--repo",
        "${workspaceFolder}"
      ]
    }
  }
}