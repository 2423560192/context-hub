# Feature: [功能名称]

文件落点：[PRD_ROOT]/[模块名]/[功能名称].md
所属模块：[模块名]
上游依赖：[无 / 依赖的模块与数据]
下游输出：[会被哪些模块使用]

## 0. 功能一句话

## 1. 业务实体与属性

| 实体 | 字段 | 类型 | 必填 | 约束 | 备注 |
|---|---|---|---|---|---|
| [实体] | [字段名] | [string/int/...] | [是/否/自动] | [长度/枚举/默认/FK/INDEX] | [中文含义] |

## 2. 行为动作链

```text
角色：谁发起操作
  -> 触发动作：用户点了什么 / 调了什么
    -> 输入：请求参数结构
      -> 校验：参数验证、权限验证、状态验证
        -> 写入：改了哪些表、哪些字段
          -> 副作用：异步任务、通知、日志
            -> 响应：返回给前端的数据结构
              -> 状态变化：操作前后实体状态变化
```

## 3. 场景矩阵（BDD 预埋）

| 场景 | 类型 | 角色 | 前置条件 | 触发 | 输入 | 副作用 | 期望结果 | 状态码 | 错误码 | BDD 文件 |
|---|---|---|---|---|---|---|---|---|---|---|
| [描述] | [Happy Path/参数非法/鉴权/越权/状态/幂等/并发/外部依赖] | [角色] | [条件] | [操作] | [入参] | [副作用] | [结果] | [状态码] | [错误码] | [.feature] |

必须覆盖：Happy Path、参数非法、鉴权失败、越权访问、状态非法、幂等/重复提交、并发冲突、外部依赖失败。

## 4. 拦截铁网

| 类型 | 场景 | 拦截条件 | 后端响应 | 错误码 | 前端表现 | 处理策略 |
|---|---|---|---|---|---|---|
| 参数 | [场景] | [条件] | [4xx] | [错误码] | [表现] | [拒绝/排队/降级/重试/忽略] |

覆盖：参数、鉴权、越权、状态、幂等、配额、并发、外部依赖。

## 5. 待确认问题

- [待确认: 需求含糊点]

## 6. 不做范围

- [明确排除的功能与实现细节]
